<h2>New loan</h2>

<%= render "form.html", changeset: @changeset,
                        action: loan_path(@conn, :create), user_id: @user_id, book_id: @book_id %>

<span><%= link "Back", to: loan_path(@conn, :index) %></span>
