<h2>New book</h2>
<%# < raw(panel_block)%>
<%= render "form.html", changeset: @changeset,
                        action: book_path(@conn, :create) %>

<span><%= link "Back", to: book_path(@conn, :index) %></span>
